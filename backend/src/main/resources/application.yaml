spring:
  application:
    name: backend
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/habitai?createDatabaseIfNotExist=True
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

  api:
    security:
      token:
        secret: ${JWT_SECRET:secretKey1234567}